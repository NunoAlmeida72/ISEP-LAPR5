describe('List Robots', () => {
    beforeEach(() => {
      // Navigate to the Angular app
      cy.visit('/list-robots'); // Update the URL as needed
    });
  
    it('should navigate through the stepper and list robots', () => {
    });
});