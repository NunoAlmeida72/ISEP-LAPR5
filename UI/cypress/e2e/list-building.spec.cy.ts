describe('list building page test',() =>{
  beforeEach(() => {
  cy.visit('/list-buildings')
  });
  
  it('should navigate through the stepper and list a building', () => {

    // Add any assertions related to the result of the floor creation if needed

  });
});